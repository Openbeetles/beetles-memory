# Beetle-Derived Legacy Fixture

This fixture represents Beetle-shaped legacy inputs after extraction:

- Beetle is a host, not a privileged SDK branch;
- subject, soul, private garden, runtime pressure, and world context remain
  first-class semantics;
- memory plane routing is still owned by Beetle Memory core and SDK contracts.
