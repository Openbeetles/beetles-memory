# Beetle-Derived SDK Host Fixture

This fixture is Beetle-shaped evidence for SDK host readiness. It is not a
kernel branch and does not introduce Beetle-specific source kinds. Tests load it
through the same public SDK candidate, migration, inspect, projection, and replay
helpers used by `generic-rust-host/`.
