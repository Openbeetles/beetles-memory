[package]
name = "bm-example-esp-standalone-memory"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
bm-adapter = { path = "../../crates/adapter", features = ["profile-esp-standalone-memory"] }
bm-entry = { path = "../../crates/entry", features = ["profile-esp-standalone-memory"] }
bm-wss = { path = "../../crates/wss", features = ["profile-esp-standalone-memory", "client-compact"] }
bm-sdk = { path = "../../crates/sdk", features = ["profile-esp-standalone-memory"] }
