[package]
name = "bm-example-esp-standalone-memory"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
bm-sdk = { path = "../../crates/sdk", features = ["profile-esp-standalone-memory"] }
