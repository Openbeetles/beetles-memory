{
  "name": "@beetle-memory/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "cargo build -p bm-llm-gateway --no-default-features --features server-async,client-reqwest && tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0"
  }
}
