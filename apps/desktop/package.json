{
  "name": "@beetle-memory/desktop",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "dev": "cargo build --locked -p bm-llm-gateway --no-default-features --features server-async,client-reqwest,profile-desktop-macos-standalone-memory && tauri dev --features profile-desktop-macos-standalone-memory",
    "build": "tauri build --features profile-desktop-macos-standalone-memory"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0"
  }
}
